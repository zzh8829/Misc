#include <iostream>
using namespace std;

const int MAX=1000;

int sum[MAX+1](int a[MAX],int b[MAX])
{
       int sum="";
       for(size_t s=0;s!=)
}

int main()
{
    int n;
    cin >> n;
    for(int ns=0;ns!=n;n++)
    {
    int a[MAX],b[MAX];
    cin >> a>>b;
    cout<< "Case "<<ns+1<<":\n"<<a<<" + "<<b<<" = "<<sum(a,b)<<endl<<endl;        
    }
    return 0;
}
